Isabelle Fite
February 8, 2025
CS-4373 High Performance Computing

# C-mini-challenges

1. For the Hello, NAME challenge, I used an argument from the command line because I wanted to learn how to work with command line arguments in C. Memory for the string is dynamically allocated since the length of the name isn't known at compile time. 

2. I timed my program with clock() from time.h. I didn't notice any issues with convergence, but looking at the output of clock, I'm at the limit of clock's precision. So for this short of a runtime, I will only be able to get one significant figure this way. 

3. 